{% extends 'base.html.twig' %}

{% block body %}
    <div class="mt-3">
        <h2> {{the_emp.nom  }} </h2>
        <small class="text-muted"> <a href="/employee"> Back to list </a> </small>
    </div>

    <table class="table">
        <tr> 
            <th> Id </th>
            <th> Service_Id </th>
            <th> Nom </th>
            <th> Prenom </th>
            <th> Recruted_At </th>
            <th> Action </th>
        </tr>

        <tr> 
            <td> {{ the_emp.id }} </td>
            <td> {{ the_emp.ServiceId.label }} </td>
            {# <td> <i> en attente </i> </td> #}
            <td> {{ the_emp.nom }} </td>
            <td> {{ the_emp.prenom }} </td>
            <td> {{ the_emp.RecrutedAt | date('d/m/y') }} </td>
            <td> 
                <a class="btn btn-primary" href="/show_employee/{{ the_emp.id }}/edit"> <img src="/img/edit2.png" width="15px" alt=""> Edit </a>
            </th>
        </tr>
            
    </table>
{% endblock %}
